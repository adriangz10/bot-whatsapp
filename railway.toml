[build]
builder = "nixpacks"

[deploy]
startCommand = "node --experimental-global-webcrypto --max-old-space-size=2048 dist/main"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3